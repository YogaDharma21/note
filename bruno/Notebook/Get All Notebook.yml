info:
  name: Get All Notebook
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/notebook/v1/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
