info:
  name: Create Notebook
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/notebook/v1/"
  body:
    type: json
    data: |-
      {
        "name": "Tutorial Coding",
        "parent_id": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
